## Initialize a Repository

Git tracks changes to your project inside a hidden `.git` folder. To create one, you run a single command.

**Your task:** Run `git init` in the terminal to initialize a new repository.

```bash
git init
```

Once you've done that, click **Check My Work**.
