const { buildUrl, formatElement } = require('./utils');

// TODO: call buildUrl('mainframe.example.com', 8080, true) and log the result

// TODO: call formatElement({ elmName: 'HELLOPGM', type: 'COBOL' }) and log the result
