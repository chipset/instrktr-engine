// TODO: write delay(ms) — returns a Promise that resolves after ms milliseconds
// hint: return new Promise(resolve => setTimeout(resolve, ms));
function delay(ms) {

}

// TODO: write runSteps() as an async function
// - log "Step 1: listing elements…", await delay(200)
// - log "Step 2: retrieving element…", await delay(200)
// - log "Done."
async function runSteps() {

}

runSteps();
