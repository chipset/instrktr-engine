<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <!-- Background: rounded square, deep navy -->
  <rect width="100" height="100" rx="16" fill="#0F1D2E"/>

  <!-- Stylized I-beam letterform -->
  <!-- Top bar -->
  <rect x="28" y="22" width="44" height="10" rx="5" fill="#4A9EFF"/>
  <!-- Stem -->
  <rect x="44" y="32" width="12" height="36" rx="0" fill="#4A9EFF"/>
  <!-- Bottom bar -->
  <rect x="28" y="68" width="44" height="10" rx="5" fill="#4A9EFF"/>

  <!-- Accent dot — bottom-right, suggests a cursor / prompt -->
  <circle cx="72" cy="73" r="6" fill="#7DD3FC"/>
</svg>
